[project]
name = "pk-exporter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.8.0",
    "pillow>=11.1.0",
    "pydantic>=2.11.0",
    "python-dotenv>=1.1.0",
    "rich>=14.0.0",
]

[tool.uv]
link-mode = "symlink"
